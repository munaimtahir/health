# Deferred items

Remaining engineering gaps: encrypted backup, PDF/report and export automated scope/share tests, full medication change-period history, complete symptom attributes and episode history, full settings/privacy/help coverage, and comprehensive UI/accessibility/security/performance tests. The vault supports private in-place replacement, metadata editing, opening and scoped sharing with recalculated MIME, size, and SHA-256 metadata; local backup includes a versioned integrity manifest; JSON and readable exports support the selected date range; and reminder scheduling reconciles persisted scheduled reminders on app initialization. The graphical sprint itself has implemented and documented the current UI refinement, but the full product remains NOT COMPLETE until the remaining gaps are closed. Local release signing is complete; Play Console submission is a manual owner action.
