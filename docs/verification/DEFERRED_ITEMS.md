# Deferred items

Remaining engineering gaps: medication change-period history, symptom flare/image/structured episode history, PDF/report and export automated scope/share tests, full settings/privacy/help coverage, and comprehensive UI/accessibility/security/performance tests. Symptom records now persist optional severity, start/end/ongoing state, duration, body location, associated symptoms, observed trigger, and related medication through the Room 5→6 migration. The vault supports private in-place replacement, metadata editing, opening and scoped sharing with recalculated MIME, size, and SHA-256 metadata; backups now use a password-derived AES-GCM envelope plus a versioned integrity manifest; JSON and readable exports support the selected date range; and reminder scheduling reconciles persisted scheduled reminders on app initialization. The graphical sprint itself has implemented and documented the current UI refinement, but the full product remains NOT COMPLETE until the remaining gaps are closed. Local release signing is complete; Play Console submission is a manual owner action.
