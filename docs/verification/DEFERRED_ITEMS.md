# Deferred items

Remaining engineering work is the final release acceptance sweep and artifact/documentation reinspection; no feature implementation is being parked. The sprint must continue until this file contains only the permitted manual Play Console submission/publication item. Medication changes now persist as a Room 6→7 history table with explicit dose-change, stop, and restart recording and are included in exports/backups. Symptom records now persist optional severity, start/end/ongoing state, duration, body location, associated symptoms, observed trigger, related medication, episode identifier, and private image attachment metadata through the Room 5→8 migrations; exact user-entered episode grouping is implemented and covered by unit tests, private attachment preservation passes on-device, and backup restore remapping now preserves the episode ID while remapping managed attachment IDs with dedicated regression tests. The vault supports private in-place replacement, metadata editing, opening and scoped sharing with recalculated MIME, size, and SHA-256 metadata; backups now use a password-derived AES-GCM envelope plus a versioned integrity manifest; JSON and readable exports support the selected date range; and reminder scheduling reconciles persisted scheduled reminders on app initialization. Report/export date scopes now share tested validation and inclusive filtering, malformed/reversed export ranges are blocked in the UI, report section selection requires at least one section, and JSON/text/PDF share MIME and sensitive-data warning policy are centralized and unit-tested. Settings/privacy/help/data-action accessibility coverage now passes on the connected TECNO CH6i, security settings include persisted 5/15/30-minute inactivity lock options in addition to lock-on-leave, the 10,000-entry summary acceptance test passes, backup tamper rejection passes, and connected biometric capability handling passes. Local release signing is complete; Play Console submission is a manual owner action.
