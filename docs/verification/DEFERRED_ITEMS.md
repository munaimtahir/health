# Deferred items

Remaining engineering gaps: symptom flare-history semantics, automated image-attachment/episode backup tests, PDF/report and export automated scope/share tests, full settings/privacy/help coverage, and comprehensive UI/accessibility/security/performance tests. These are active engineering work, not stopping points. The sprint must continue gap-by-gap until this file contains only the permitted manual Play Console submission/publication item. Medication changes now persist as a Room 6→7 history table with explicit dose-change, stop, and restart recording and are included in exports/backups. Symptom records now persist optional severity, start/end/ongoing state, duration, body location, associated symptoms, observed trigger, related medication, episode identifier, and private image attachment metadata through the Room 5→8 migrations. The vault supports private in-place replacement, metadata editing, opening and scoped sharing with recalculated MIME, size, and SHA-256 metadata; backups now use a password-derived AES-GCM envelope plus a versioned integrity manifest; JSON and readable exports support the selected date range; and reminder scheduling reconciles persisted scheduled reminders on app initialization. Local release signing is complete; Play Console submission is a manual owner action.
