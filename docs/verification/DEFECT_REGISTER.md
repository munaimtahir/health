# Defect Register

| Defect ID | Severity | Sprint | Area | Description | Reproduction | Expected | Actual | Root cause | Fix | Regression test | Status |
|---|---:|---:|---|---|---|---|---|---|---|---|---|
| DEF-001 | 0 | 0 | Build | KSP target differed from Java target on host JDK 21 | Run initial Gradle gate | Build proceeds with Java 17 targets | `kspDebugKotlin` failed with JVM-target mismatch | Root build did not configure Kotlin target | Root `build.gradle.kts` now applies JVM 17 to Kotlin compile tasks | Full Gradle test/build rerun pending | FIXED |
| DEF-002 | 3 | 0 | Emulator | Emulator launch displayed System UI not-responding dialog during smoke launch | Install/launch on `emulator-5554` | App launch can be inspected | Dialog appeared; ActivityManager still showed app resumed | Environmental System UI condition not reproduced on physical target | Repeat emulator after environment stabilizes | NOT REPRODUCED — re-checked against multiple independent clean `connectedCheck` runs on the same `emulator-5554` serial (API 26 AVD) plus manual logcat inspection; no ANR/"not responding" dialog observed in any run. Left OPEN rather than FIXED since nothing was code-changed to address it and a one-time environmental condition can't be proven never to recur, but current evidence shows it is not persistent. |
| DEF-003 | 2 | 0 | Android tests | Connected test used unavailable `ActivityTestRule` and lacked test-rules dependency | Run `connectedCheck` | Instrumentation compiles and runs | Android test compilation failed | Obsolete AndroidX test API | Switched to `ActivityScenarioRule`, added test rules, asserted activity non-null | Connected gate passed on attached devices | FIXED |
| DEF-004 | 3 | 0 | Build resources | Multi-module connected dex merge exhausted default heap | Run connected gate | Test APKs merge | D8 Java heap exhaustion | 2 GiB heap and unconstrained workers | 4 GiB heap and two Gradle workers | `connectedCheck` and `verify_project.sh` passed | FIXED |
| DEF-005 | 3 | 1 | Reminders | Reminder due during forced-Doze idle did not fire within a 120s host-driven acceptance window on the API 26 emulator | `dumpsys deviceidle force-idle`, schedule a reminder due in 15s, poll for 120s | Reminder fires or is deferred to a bounded, provable maintenance window | Timed out twice; root-caused partly to test methodology (retry after the first attempt's own cleanup had already deleted the fixture row) and partly to `force-idle` never naturally reaching an `IDLE_MAINTENANCE` window (`dumpsys deviceidle step` also failed to progress past ACTIVE on this emulator image even with battery unplugged and screen off) | Plain `OneTimeWorkRequest`s are, by Android platform design, deferred during Doze until a maintenance window unless marked expedited (API 31+, unavailable at this app's minSdk 26); this is standard platform behavior, not necessarily an app defect, but was not conclusively distinguished from a real reliability gap in this environment | None applied — out of scope to redesign reminder delivery around foreground-service or expedited-work exemptions without product input | The same reminder, scheduled fresh under normal (non-doze) conditions, reliably fired and posted a notification (`ReminderSystemAcceptanceTest#verify_doze_reminder_fires` passed once doze was lifted); reboot survival separately verified clean via a real `adb reboot` cycle | INCONCLUSIVE — durable persistence and reboot survival are verified; live behavior specifically during Doze is not, and is a documented follow-up rather than a claimed pass |
| DEF-006 | 2 | — | Visual/Theming | Status bar icons (clock, battery, wifi) are invisible on Android 16 (API 36) | Install debug APK on `Android_16_Test` emulator (`google_apis_playstore/x86_64`, API 36), complete onboarding, view Home/Records/Plan/Vault/Profile | Status bar icons remain legible against the app's light top-bar/background on every screen | Status bar renders as solid white with zero visible icon/text content on Home, Records, Plan, and Vault (light theme); reproduced on every main tab | `app/src/main/res/values/themes.xml` sets `Theme.VexelHealthPassport` to parent `Theme.AppCompat.Light.NoActionBar` with no `android:windowLightStatusBar` and no runtime `WindowInsetsControllerCompat.isAppearanceLightStatusBars` call; `MainActivity.kt` never calls `enableEdgeToEdge()` or sets bar appearance. Under Android 15+ (API 35+) mandatory edge-to-edge enforcement, `Window.setStatusBarColor` is a no-op and the status bar is transparent over app content; with icon appearance left unset it defaults to light/white icons, which are invisible against this app's light-colored top bars. Not reproduced on the physical Tecno device (Android 13) or the API 26 emulator because neither enforces edge-to-edge, so the pre-existing default rendered icons legibly by coincidence, not by explicit handling. | `core/designsystem/src/main/kotlin/com/vexel/passport/core/designsystem/Theme.kt`: `VexelHealthPassportTheme` now resolves the hosting `Activity` from `LocalView.current.context` and, in a `SideEffect`, sets `WindowCompat.getInsetsController(window, view).isAppearanceLightStatusBars` and `.isAppearanceLightNavigationBars` to `!darkTheme` — driven by the same app-level `darkTheme` flag the theme already receives (not system dark mode), so it tracks the in-app "Use dark theme" toggle. Added `androidx.core:core-ktx` to `core/designsystem/build.gradle.kts` for `WindowCompat`. | Rebuilt, reinstalled on the `Android_16_Test` (API 36) emulator, and re-verified manually: status bar icons are dark/legible in light theme and light/legible in dark theme on Home and Profile, toggling correctly via the in-app "Use dark theme" control. `./gradlew :app:testDebugUnitTest :core:designsystem:testDebugUnitTest :app:lintDebug :core:designsystem:lintDebug` all pass. Evidence: `evidence/visual-2026-08-09/emulator-api36-profile-light-fixed.png`, `evidence/visual-2026-08-09/emulator-api36-profile-dark-fixed.png` | FIXED |
