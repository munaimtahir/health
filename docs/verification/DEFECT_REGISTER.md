# Defect Register

| Defect ID | Severity | Sprint | Area | Description | Reproduction | Expected | Actual | Root cause | Fix | Regression test | Status |
|---|---:|---:|---|---|---|---|---|---|---|---|---|
| DEF-001 | 0 | 0 | Build | KSP target differed from Java target on host JDK 21 | Run initial Gradle gate | Build proceeds with Java 17 targets | `kspDebugKotlin` failed with JVM-target mismatch | Root build did not configure Kotlin target | Root `build.gradle.kts` now applies JVM 17 to Kotlin compile tasks | Full Gradle test/build rerun pending | FIXED |
| DEF-002 | 3 | 0 | Emulator | Emulator launch displayed System UI not-responding dialog during smoke launch | Install/launch on `emulator-5554` | App launch can be inspected | Dialog appeared; ActivityManager still showed app resumed | Environmental System UI condition not reproduced on physical target | Repeat emulator after environment stabilizes | NOT REPRODUCED — re-checked against multiple independent clean `connectedCheck` runs on the same `emulator-5554` serial (API 26 AVD) plus manual logcat inspection; no ANR/"not responding" dialog observed in any run. Left OPEN rather than FIXED since nothing was code-changed to address it and a one-time environmental condition can't be proven never to recur, but current evidence shows it is not persistent. |
| DEF-003 | 2 | 0 | Android tests | Connected test used unavailable `ActivityTestRule` and lacked test-rules dependency | Run `connectedCheck` | Instrumentation compiles and runs | Android test compilation failed | Obsolete AndroidX test API | Switched to `ActivityScenarioRule`, added test rules, asserted activity non-null | Connected gate passed on attached devices | FIXED |
| DEF-004 | 3 | 0 | Build resources | Multi-module connected dex merge exhausted default heap | Run connected gate | Test APKs merge | D8 Java heap exhaustion | 2 GiB heap and unconstrained workers | 4 GiB heap and two Gradle workers | `connectedCheck` and `verify_project.sh` passed | FIXED |
