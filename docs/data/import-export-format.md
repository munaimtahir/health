# Import and Export Format

Status: Draft. Purpose: reserve a versioned, inspectable format.

The future export format will include a versioned manifest, structured JSON data, and referenced original files. It must include checksums, schema version, timestamps, and no implicit network dependency. Exact schema remains unresolved until feature implementation.

