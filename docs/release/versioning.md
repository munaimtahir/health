# Versioning

Status: Canonical. Purpose: define initial versioning.

Use Android `versionCode` as a monotonically increasing integer and `versionName` as a human-readable semantic version. Initial scaffold is `0.1.0`/code 1 until a release decision is approved.

