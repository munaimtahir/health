# Database Migration Tests

Status: Draft. Purpose: require migration evidence.

Each schema version must have Room migration tests, clean open/reopen tests, foreign-key and transaction checks, and restore compatibility checks. No destructive migration is accepted as the production default.

